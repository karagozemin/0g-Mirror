{
  "name": "0g-mirror",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "contracts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "compile": "npm run compile --workspace contracts",
    "test": "npm run test --workspace contracts",
    "deploy": "npm run deploy --workspace contracts",
    "deploy:0g": "npm run deploy:0g --workspace contracts",
    "proof:real": "node scripts/create-real-proof.mjs",
    "screenshots": "node scripts/capture-screenshots.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  }
}
